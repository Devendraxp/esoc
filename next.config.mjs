/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com', 'ui-avatars.com','img.clerk.com',"images.pexels.com"],
  },
};

export default nextConfig;
